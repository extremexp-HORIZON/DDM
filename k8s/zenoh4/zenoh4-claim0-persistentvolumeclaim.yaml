apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: zenoh4-claim0
  name: zenoh4-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 250Mi
  storageClassName: manual
---
apiVersion: v1
kind: PersistentVolume
metadata:
  # annotations:
  #   hostPathProvisionerIdentity: zenohcluster1
  #   pv.kubernetes.io/provisioned-by: microk8s.io/hostpath
  # finalizers:
  # - kubernetes.io/pv-protection
  name: zenoh4-pv
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 250Mi
  #  claimRef:
  #    apiVersion: v1
  #    kind: PersistentVolumeClaim
  #    name: zenoh4-claim0
  hostPath:
    path: /home/karagk/volume/DDM/zenoh4
    # type: Directory
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - extremexp-171
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual
  volumeMode: Filesystem
